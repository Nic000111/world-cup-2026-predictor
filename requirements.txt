# Pinned to known-good majors. Streamlit Cloud auto-grabs the newest match;
# without these caps it pulled pandas 3.0 / Python 3.14 which broke runtime tabs.
streamlit>=1.28,<2
pandas>=2.0,<3
numpy>=1.24,<3
scikit-learn>=1.3,<2
scipy>=1.10,<2
