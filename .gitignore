# Python
__pycache__/
*.pyc
*.pyo
.venv*/
venv/
.ipynb_checkpoints/

# Streamlit secrets (NEVER commit — holds your password)
.streamlit/secrets.toml

# Per-deployment state — keep local entries local, deployed entries deployed
manual_results.csv

# OS / editor noise
.DS_Store
.vscode/
.idea/

# Local-only deliverables (saved to ~/Downloads anyway)
*.command
data/
downloaded_files/
